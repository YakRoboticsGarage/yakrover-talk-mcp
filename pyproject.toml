[project]
name = "talk-mcp"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
  "mcp[cli]",
  "fastmcp",
  "elevenlabs>=1.0.0",
  "python-dotenv",
]

[tool.setuptools]
py-modules = ["server", "config", "prompts", "tts", "audio"]

[project.scripts]
talk-mcp = "server:main"
